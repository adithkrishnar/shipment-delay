import {useEffect,useState} from 'react';import {BrowserRouter,Routes,Route} from 'react-router-dom';import Sidebar from './components/Sidebar';import Topbar from './components/Topbar';import {getCompanies,seedDemo} from './services/api';import Overview from './pages/Overview';import Shipments from './pages/Shipments';import Demand from './pages/Demand';import Inventory from './pages/Inventory';import Suppliers from './pages/Suppliers';import Anomalies from './pages/Anomalies';import Simulator from './pages/Simulator';import Recommendations from './pages/Recommendations';import Models from './pages/Models';import Data from './pages/Data';
export default function App(){const[companies,setCompanies]=useState([]);const[id,setId]=useState(Number(localStorage.getItem('supplyiq_company')||0));const[loading,setLoading]=useState(true);const[err,setErr]=useState('');const load=()=>getCompanies().then(x=>{setCompanies(x);if(!id&&x[0]){setId(x[0].id);localStorage.setItem('supplyiq_company',x[0].id)}}).catch(e=>setErr(e.response?.data?.detail||e.message)).finally(()=>setLoading(false));useEffect(()=>{load()},[]);const seed=()=>{setLoading(true);seedDemo().then(load).catch(e=>{setErr(e.response?.data?.detail||e.message);setLoading(false)})};const company=companies.find(x=>x.id===id)||companies[0];if(loading)return <div className="boot"><div className="brand-mark">S</div><h2>SupplyIQ</h2><p>Starting intelligence layer…</p></div>;if(!companies.length)return <div className="boot"><div className="brand-mark">S</div><h2>No company data yet</h2><p>Seed the realistic demo companies to start.</p><button className="primary" onClick={seed}>Seed demo data</button>{err&&<p className="error">{err}</p>}</div>;const changeCompany=x=>{setId(x);localStorage.setItem('supplyiq_company',x)};return <BrowserRouter><div className="app"><Sidebar/><main className="main"><Topbar company={company} companies={companies} onCompany={changeCompany}/><Routes><Route path="/" element={<Overview company={company}/>}/><Route path="/shipments" element={<Shipments company={company}/>}/><Route path="/demand" element={<Demand company={company}/>}/><Route path="/inventory" element={<Inventory company={company}/>}/><Route path="/suppliers" element={<Suppliers company={company}/>}/><Route path="/anomalies" element={<Anomalies company={company}/>}/><Route path="/simulator" element={<Simulator company={company}/>}/><Route path="/recommendations" element={<Recommendations company={company}/>}/><Route path="/models" element={<Models company={company}/>}/><Route path="/data" element={<Data company={company}/>}/></Routes></main></div></BrowserRouter>}
