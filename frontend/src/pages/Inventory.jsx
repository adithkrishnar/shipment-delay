import {useEffect,useState} from 'react';import {getInventory} from '../services/api';import Panel from '../components/Panel';import Loader from '../components/Loader';import RiskBadge from '../components/RiskBadge';
export default function Inventory({company}){const[d,setD]=useState(null);useEffect(()=>{if(company)getInventory(company.id).then(setD)},[company]);if(!d)return <Loader/>;return <div className="page"><div className="page-title"><span className="eyebrow">INVENTORY INTELLIGENCE</span><h2>See what can run out — and what is sitting idle.</h2></div><div className="metrics"><div className="metric"><span>Inventory units</span><strong>{d.summary.inventory_units.toLocaleString()}</strong></div><div className="metric"><span>High stockout risk</span><strong>{d.summary.stockout_high}</strong></div><div className="metric"><span>High overstock risk</span><strong>{d.summary.overstock_high}</strong></div></div><Panel title="Product inventory health"><div className="table-wrap"><table><thead><tr><th>Product</th><th>Inventory</th><th>Daily demand</th><th>Coverage</th><th>Stockout</th><th>Reorder</th></tr></thead><tbody>{d.products.map(x=><tr key={x.product_id}><td><b>{x.product_name}</b><small>{x.category}</small></td><td>{x.inventory_level.toLocaleString()}</td><td>{x.daily_demand}</td><td>{x.inventory_coverage_days??'∞'} days</td><td><RiskBadge risk={x.stockout_risk}/><small>{(x.stockout_probability*100).toFixed(0)}%</small></td><td>{x.recommended_order_quantity.toLocaleString()}</td></tr>)}</tbody></table></div></Panel></div>}
