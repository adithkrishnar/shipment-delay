import { useEffect, useState } from 'react'; import { getModels, retrain } from '../services/api'; import Panel from '../components/Panel'; import Loader from '../components/Loader';
export default function Models({ company }) { const [d, setD] = useState(null); const [msg, setMsg] = useState(''); const load = () => company && getModels(company.id).then(setD); useEffect(() => { load() }, [company]); if (!d) return <Loader />; return <div className="page"><div className="page-title"><span className="eyebrow">MODEL CENTER</span><h2>Track training, versions and real test metrics.</h2></div><button className="primary" onClick={() => retrain(company.id).then(x => { setMsg('Retraining complete'); load() }).catch(e => setMsg(e.response?.data?.detail || e.message))}>Retrain company models</button>{msg && <p className="callout">{msg}</p>}<Panel title="Model registry"><div className="table-wrap"><table><thead><tr><th>Type</th><th>Source</th><th>Version</th><th>Rows</th><th>Metrics</th><th>Status</th></tr></thead><tbody>{d.map(m => <tr key={m.id}><td>{m.model_type}</td><td>{m.model_source}</td><td>{m.version}</td><td>{m.dataset_size || '—'}</td><td><small>{JSON.stringify(m.metrics || {}).slice(0, 150)}</small></td><td>{m.status}</td></tr>)}</tbody></table></div></Panel></div> }
