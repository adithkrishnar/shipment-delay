import {useEffect,useState} from 'react';import {getRecommendations} from '../services/api';import Panel from '../components/Panel';import Loader from '../components/Loader';import RiskBadge from '../components/RiskBadge';
export default function Recommendations({company}){const[d,setD]=useState(null);useEffect(()=>{if(company)getRecommendations(company.id).then(setD)},[company]);if(!d)return <Loader/>;return <div className="page"><div className="page-title"><span className="eyebrow">AI RECOMMENDATIONS</span><h2>Turn risk signals into actions.</h2></div><Panel title="Prioritized actions"><div className="recommendations">{d.recommendations.map((r,i)=><div className="recommendation" key={i}><RiskBadge risk={r.priority}/><div><b>{r.title}</b><p>{r.reason}</p><small>{r.expected_impact}</small></div></div>)}</div></Panel></div>}
